{
    "name": "cf_ai_currency_helper", 
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "format": "prettier -w ."
    },
    "devDependencies": {
        "wrangler": "^3.80.0",
        "prettier": "^3.3.3"
    }
}